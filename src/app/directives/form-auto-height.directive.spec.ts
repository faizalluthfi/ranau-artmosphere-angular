import { FormAutoHeightDirective } from './form-auto-height.directive';

describe('FormAutoHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FormAutoHeightDirective();
    expect(directive).toBeTruthy();
  });
});
