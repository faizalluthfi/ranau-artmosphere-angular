import { ListAutoHeightDirective } from './list-auto-height.directive';

describe('ListAutoHeightDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ListAutoHeightDirective();
  //   expect(directive).toBeTruthy();
  // });
});
